1. read_file(filename): (operands, operators, result): data
2. write_file(filename, number)
3. solve(data, csp): {character: number} or None

4. create_csp(data)
5. same_operand(data, csp)
6. (data, csp) // tổng cột đầu lớn hơn 10

7. backtrack(data, assignment, csp): true or false
8. degree_heuristic(csp): character
9. inference(csp, var, val): (csp_removed, status): tuple

10. sum_cryptarithmetic(data, assignment): true or false




data: (operands, operators, result): tuple

csp:
	variables = [] // operand + result
	variables = { // operand
		character: (count_constraint, status)
	}
	constraints: ["", "", ""]
	domains = { character: [numbers] }

assignment = { character: number }

clone_domains_of_variable: use deepcopy

inferences = (
	csp_removed: { character: number },
	status: true
): tuple
// status là true nếu còn gán được